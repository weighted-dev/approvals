{
  "name": "weighted-approvals-monorepo",
  "private": true,
  "scripts": {
    "build": "pnpm --filter @weighted-approvals/action build:all",
    "test": "pnpm --filter @weighted-approvals/action test",
    "dev": "pnpm --parallel --filter './apps/*' dev"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.15.0"
}
